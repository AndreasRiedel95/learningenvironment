<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>MY APP</title>
</head>
<body>
<h1><%= title %></h1>
<% if(section_list.length > 0) { %>
	<% for (var i=0; i<section_list.length; i++) {%>
		<p><%= section_list[i].name %></p>
	<% } %>
<% } else { %>
	<p>There are no sections</p>
<% } %>
</body>
</html>